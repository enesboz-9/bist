name: Keep Streamlit Alive

on:
  schedule:
    # Her 6 saatte bir çalışır (00:00, 06:00, 12:00, 18:00)
    - cron: '0 */6 * * *'
  workflow_dispatch: # Manuel olarak da başlatabilmen için bu seçeneği ekledim

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -I https://liicwet2rsxjrxjmtbvt5w.streamlit.app/
          echo "Ping basarili: $(date)"
